import 'package:flutter/material.dart';
import 'package:health_taylor/open_api/list.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListWidget(),
    );
  }
}
